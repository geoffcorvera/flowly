# @visx/* and some other deps still declare React 16-18 peer ranges, but the app
# runs React 19 (works fine at runtime). Allow installs to proceed past the peer
# mismatch so `npm ci` succeeds locally and in CI.
legacy-peer-deps=true
